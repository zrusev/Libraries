const chartService = function () {
  generator.load('#content-wrapper', constants.chartWrapper);

  generator.load('#page-data-wrapper', constants.pageDataWrapper);
  
  $('#countryButton').click(canvas);
}